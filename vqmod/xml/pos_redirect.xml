<modification>    <id>POS Auto Redirect</id>    <version>1.0</version>    <vqmver>2.x</vqmver>    <author>Jimmy: flyingmel.java@gmail.com, Jason: hh4cimm@yahoo.com</author>    <file name="admin/controller/common/login.php">		<operation>			<search position="replace"><![CDATA[$this->redirect($this->url->link('common/home', 'token=' . $this->session->data['token'], 'SSL'));]]></search>
			<add><![CDATA[$this->load->model('setting/setting');			$pos_settings = $this->model_setting_setting->getSetting('POS');			$excluded_groups = array();			if (isset($pos_settings['excluded_groups'])) {				$excluded_groups = $pos_settings['excluded_groups'];			}			$this->load->model('user/user');			$user = $this->model_user_user->getUser($this->user->getId());			$user_group_id = 0;			if ($user) {				$user_group_id = $user['user_group_id'];			}			if ($pos_settings && isset($pos_settings['display_once_login']) && $pos_settings['display_once_login'] == '1'				&& !in_array($user_group_id, $excluded_groups)) {				$this->redirect($this->url->link('module/pos/main', 'token=' . $this->session->data['token'], 'SSL'));			} else {				$this->redirect($this->url->link('common/home', 'token=' . $this->session->data['token'], 'SSL'));			}]]></add>
		</operation>    </file></modification>
